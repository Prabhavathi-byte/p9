on:
push:
branches:["main]

jobs:
build:
runs-on:windows-latest

steps:
name:checkout code
uses:actions/checkout@v4

name:Setup python
uses:actions/setup-python@v5

name:Run python file
run:python app.py
